package com.example.currencyconverter.db;

/**
 * Stores database data.
 *
 * @author Vladislav
 * @since 1.2
 * @version 1.2
 */
final class DBConfig {
    /**
     * A database's name.
     */
    static final String DATABASE = "rates.db";
    /**
     * A response's table name.
     */
    static final String RESPONSE = "response";
    /**
     * An id's column name.
     */
    static final String ID = "id";
    /**
     * A json's column name.
     */
    static final String JSON = "json";
    /**
     * A timestamp's column name.
     */
    static final String TIMESTAMP = "timestamp";
}

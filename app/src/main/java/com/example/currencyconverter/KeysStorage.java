package com.example.currencyconverter;

/**
 * Stores bundle keys.
 *
 * @author Vladislav
 * @version 1.2
 * @since 1.0
 */
final class KeysStorage {
    /**
     * An exchange rates API server's key.
     */
    protected static final String API_KEY = "";
    /**
     * A current locale date's bundle key.
     */
    protected static final String CURRENT_DATE = "CURRENT_DATE";
    /**
     * A selected exchange currency's bundle key.
     */
    protected static final String EXCHANGE_CURRENCY = "EXCHANGE_CURRENCY";
    /**
     * A selected exchange currency's sign's bundle key.
     */
    protected static final String EXCHANGE_CURRENCY_SIGN = "EXCHANGE_CURRENCY_SIGN";
    /**
     * A JSON response's from an exchange rates API server bundle key.
     */
    protected static final String JSON = "JSON";
    /**
     * A currency tile's title's bundle key.
     */
    protected static final String BUTTON_TEXT = "BUTTON_TEXT";
}
